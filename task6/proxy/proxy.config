server {
    listen 80;

    # Front-end requests
    location / {
        proxy_pass http://front-end:9000;
    }

    # Back-end API requests
    location /api/ {
        proxy_pass http://back-end:5252;
    }
}
